# 叙思织绣独立站 - 详细实施计划

> 文档版本：v1.1  
> 更新日期：2026-06-19  
> 项目路径：`/Users/andy_dongcheng/Desktop/DC_WORK/WORKSPACE/16_文娱产业项目/00_web`

---

## 1. 文档体系说明

本项目维护以下文档，各司其职：

| 文档 | 用途 | 维护频率 |
|-----|------|---------|
| `PRD.md` | 产品需求文档，描述品牌定位、用户、信息架构、功能规范、设计风格 | 阶段性更新 |
| `USER_STORIES.md` | 用户故事，从用户视角描述需求与验收标准 | 随功能新增/调整更新 |
| `implementation_plan.md` | 详细实施计划，按阶段列出任务、技术方案、依赖、验收标准 | 每阶段开始前细化 |
| `PROJECT_STATUS.md` | 项目当前状态快照，含数据现状、已完成功能、待办清单 | 每周或每阶段结束时更新 |
| `PROJECT_ISSUES.md` | 项目问题、技术债务、风险、阻塞项 | 发现问题时即时更新 |

---

## 2. 实施原则

1. **内容源唯一**：`kb/`（Obsidian Vault）是藏品与产品内容的唯一来源，所有内容修改通过 Markdown 笔记完成，再经 `build_kb.py` 同步。
2. **数据驱动**：重要决策前先用现有 2124 条藏品数据做验证或 POC。
3. **渐进式增强**：优先做成本低、体验提升明显的功能（如 AI 评鉴全量生成），再推进架构级改造（如部署、支付）。
4. **可回滚**：批量生成/修改前先备份 `kb/` 目录。
5. **文档先行**：每个阶段开始前，确保对应需求在 `PRD.md` 和用户故事中已明确。

---

## 3. 第一阶段：立即执行

### 3.1 AI 艺术评鉴全量生成

**关联用户故事**：US-003、US-008、US-018

#### 目标
为全部 2124 条藏品生成高质量 AI 艺术评鉴，并在详情页展示；同时新增「基于图片的推荐卡片」。

#### 范围
- 调用 Kimi API 批量生成评鉴
- 生成内容写入 `kb/Collections/**/*.md` 的 `appreciation` 字段
- 更新 `detail.html` 展示样式
- 设计并实现推荐卡片组件
- 生成二维码占位图并固定路径

#### 技术方案
1. **批量生成脚本**
   - 基于 `scripts/ai_appreciation.py`，扩展 Kimi 调用为默认正式方案
   - 增加断点续跑：记录已生成笔记的 ID 到 `data/appreciation_progress.json`
   - 增加失败重试：单条失败时重试 3 次，仍失败则记录到日志
   - 并发控制：建议 5–10 并发，避免触发 API 限流

2. **提示词模板**
   - 固定提示词，确保输出风格统一
   - 输入：藏品名称、描述、质地、年代、来源
   - 输出：200 字左右、语言典雅、突出工艺/纹样/历史意蕴/审美价值

3. **详情页展示**
   - 在 `detail.html` 的「专家解读」区块展示评鉴
   - 如果评鉴为空或为「（待生成）」，则不展示或显示占位提示

4. **推荐卡片**
   - 位置：评鉴下方
   - 数据来源：基于当前藏品标签/来源/年代的相似藏品（先用简单规则，后续可升级为向量相似）
   - 样式：卡片居中，横向滑动或 4 列网格，简约典雅
   - 单张卡片：主图 + 名称 + 来源/年代

5. **二维码占位**
   - 路径：`assets/images/xusi/wechat-qrcode.png`
   - 先生成占位二维码图片，后续替换为真实二维码即可全站生效

#### 依赖
- Kimi API Key 已配置
- `kb/` 笔记结构稳定
- `detail.html` 已完成动态加载

#### 验收标准
- [x] 2124 条藏品中 1577 条（74.3%）成功生成评鉴；剩余 547 条因原始描述缺失暂无法生成
- [x] 生成后的评鉴不出现基础信息复述
- [x] 详情页「专家解读」区块正常展示
- [x] 推荐卡片展示 4–6 个相关藏品，样式居中、美观
- [x] 二维码占位图路径统一，全站咨询弹窗引用该路径

#### 时间估算
- 脚本改造与测试：1 天
- 批量生成（2124 条）：1–2 天（取决于 API 速度与限流）
- 详情页与推荐卡片开发：2 天
- 二维码占位与弹窗统一：0.5 天
- **合计：4–6 天**

---

### 3.2 多语言完善

**关联用户故事**：US-021

#### 目标
实现中英双语切换，优先覆盖首页、导航、检索页、详情页核心文案。

#### 范围
- 设计多语言切换组件（导航栏右上角）
- 提取需要翻译的文案到 JSON 文件
- 实现 JS 语言切换逻辑
- 翻译核心页面文案

#### 技术方案
1. **语言文件**
   - `assets/locales/zh.json`
   - `assets/locales/en.json`
   - 按页面/模块组织 key，如 `home.hero.title`、`search.placeholder`

2. **切换逻辑**
   - 使用 `data-i18n` 属性标记可翻译元素
   - JS 读取 `localStorage` 中的语言设置
   - 切换时遍历 DOM 替换文本
   - 刷新页面后保持选择

3. **翻译范围（第一阶段）**
   - 导航栏所有链接
   - 首页 Hero、核心数据、页脚
   - 检索页标题、输入框占位、搜索按钮、标签云标题
   - 详情页基础信息标签、按钮、区块标题
   - 暂不翻译藏品名称、描述、评鉴等动态内容

#### 依赖
- 静态页面结构稳定

#### 验收标准
- [x] 导航栏有语言切换按钮（中/EN）
- [x] 切换语言后，核心文案即时更新
- [x] 刷新页面后语言选择保持
- [x] 英文文案无乱码、无截断

#### 时间估算
- 框架搭建：1 天
- 文案提取与翻译：1–2 天
- 页面接入与测试：1 天
- **合计：3–4 天**

---

### 3.3 标签推荐算法优化（第一版）

**关联用户故事**：US-006、US-020

#### 目标
在现有基于结果频率的标签推荐基础上，引入更精准的加权机制。

#### 范围
- 后端或前端实现标签共现、TF-IDF、Embedding 相似度中的至少一种加权
- 替换或增强 `search.html` 中的 `updateRecommendedTags` 函数

#### 技术方案
1. **方案选择（推荐组合）**
   - **共现加权**：统计历史上同时出现的标签对，提升共现标签的推荐分数
   - **TF-IDF 加权**：降低高频通用标签（如「丝」）的权重，提升有区分度的标签
   - 先实现前两种，Embedding 相似度作为第二版

2. **数据预计算**
   - 新增 `scripts/build_tag_weights.py`
   - 输入：`data/collections.json` 或 SQLite
   - 输出：`data/tag_weights.json`，包含：
     - 标签共现矩阵
     - 每个标签的 IDF 值
     - 标签到藏品的倒排索引

3. **前端推荐逻辑**
   - 搜索后拿到 Top-K 结果
   - 对每个候选标签计算得分：`score = freq * cooccurrence_boost * idf`
   - 过滤已选标签，按得分排序取前 30

#### 依赖
- `data/collections.json` 可用
- 搜索接口正常返回 tags

#### 验收标准
- [x] 推荐标签与查询意图更相关
- [x] 高频通用标签不再垄断前排
- [x] 搜索不同查询时，推荐标签有明显差异
- [x] 不增加搜索响应时间超过 200ms

#### 时间估算
- 算法设计与数据预计算脚本：2 天
- 前端接入与测试：1 天
- **合计：3 天**

---

### 3.4 数据分析页面（静态版）

**关联用户故事**：US-019

#### 目标
新增一个静态数据分析页面，展示藏品内容建设的基础指标。

#### 范围
- 新增 `analytics.html`
- 从 `data/collections.json` / `tags.json` 读取数据并可视化
- 展示藏品总数、来源分布、标签分布、年代分布等

#### 技术方案
1. **页面结构**
   - 沿用现有导航与页脚
   - 主体为数据卡片 + 简单图表

2. **数据来源**
   - `data/collections.json`
   - `data/tags.json`
   - 通过 `fetch` 加载 JSON 后在前端聚合统计

3. **可视化**
   - 使用轻量级图表库（如 Chart.js 或 ECharts），或纯 CSS 实现
   - 图表类型：
     - 来源分布：饼图/环形图
     - 年代分布：柱状图
     - Top 20 标签：横向条形图
     - 关键指标卡片：总数、标签数、图片数、评鉴完成率

4. **部署**
   - 页面为静态，可直接通过搜索服务或静态服务器访问
   - 路径：`/analytics.html`

#### 依赖
- `data/collections.json` 与 `tags.json` 已生成
- 可选引入图表库

#### 验收标准
- [x] 页面可正常访问
- [x] 展示藏品总数、图片总数、标签总数
- [x] 展示来源分布、年代分布、Top 标签
- [x] 样式与站点整体风格一致

#### 时间估算
- 页面框架与数据聚合：1 天
- 图表实现与样式调整：1–2 天
- **合计：2–3 天**

---

## 4. 第二阶段：后续完善

### 4.1 微信二维码替换

**关联用户故事**：US-004、US-014

#### 目标
将全站咨询弹窗中的占位二维码替换为真实微信号二维码。

#### 技术方案
- 已在第一阶段将路径统一为 `assets/images/xusi/wechat-qrcode.png`
- 直接覆盖该文件为真实二维码图片
- 检查 `index.html`、`search.html`、`detail.html` 中的弹窗引用

#### 验收标准
- [ ] 全站咨询弹窗展示真实二维码
- [ ] 二维码可扫描添加微信

#### 时间估算
- **0.5 天**

---

### 4.2 在线支付

**关联用户故事**：US-011

#### 目标
接入微信支付/支付宝，支持文创周边直接购买。

#### 技术方案
1. **后端服务**
   - 扩展 `search_server.py` 或新增 `order_server.py`
   - 订单表：`orders`（id、user_id、product_id、amount、status、created_at）

2. **支付接入**
   - 微信支付：JSAPI / Native / H5
   - 支付宝：手机网站支付 / 当面付
   - 需要申请商户号并配置密钥

3. **前端流程**
   - 自营产品详情页增加「立即购买」按钮
   - 订单确认页 → 支付页 → 支付结果页

#### 依赖
- 用户系统（至少匿名订单或简单手机号）
- 真实商户号
- 后端服务可公网访问

#### 验收标准
- [ ] 可创建订单
- [ ] 可调起微信支付/支付宝
- [ ] 支付回调更新订单状态

#### 时间估算
- **1–2 周**

---

### 4.3 用户系统

**关联用户故事**：US-011

#### 目标
实现会员注册、登录、收藏、订单管理。

#### 技术方案
1. **数据库**
   - 扩展 SQLite：`users`、`favorites`、`orders` 表

2. **认证**
   - 手机号 + 验证码登录
   - 或微信扫码登录（需微信开放平台）

3. **功能**
   - 注册/登录
   - 收藏藏品/产品
   - 查看订单历史

#### 依赖
- 后端服务部署到公网
- 短信服务或微信登录配置

#### 验收标准
- [ ] 用户可注册登录
- [ ] 可收藏藏品
- [ ] 可查看订单

#### 时间估算
- **1–2 周**

---

### 4.4 搜索服务部署方案

#### 目标
解决当前搜索服务仅限本地的问题。

#### 技术方案
**方案 A：预生成静态 JSON（推荐先做）**
- 定时运行脚本，将搜索接口结果预生成 JSON 文件
- 前端直接读取 JSON，部署到 CDN
- 优点：简单、成本低
- 缺点：非实时，适合内容稳定的藏品库

**方案 B：云端 FastAPI**
- 将 `search_server.py` 部署到阿里云/腾讯云/VPS
- 优点：实时搜索
- 缺点：需要服务器与运维

**方案 C：Serverless**
- 使用阿里云函数计算、AWS Lambda 等
- 优点：按需付费
- 缺点：FAISS 加载可能有冷启动问题

#### 验收标准
- [ ] 网站可在公网访问
- [ ] 搜索功能在公网可用

#### 时间估算
- **方案 A：2–3 天**
- **方案 B/C：1–2 周**

---

### 4.5 CMS 接入

#### 目标
让运营人员无需直接编辑 Markdown 即可更新产品、活动、文物内容。

#### 技术方案
- **轻量级方案**：继续使用 Obsidian + Git，通过 Git 同步更新
- **可视化 CMS**：接入 Decap CMS（原 Netlify CMS）或 TinaCMS
- 需要后端存储支持（如 Git 仓库、云存储）

#### 验收标准
- [ ] 运营人员可通过界面编辑内容
- [ ] 编辑后自动同步到网站

#### 时间估算
- **1–2 周**

---

### 4.6 自营产品详情页

**关联用户故事**：US-010

#### 目标
在知识库中单独构建「产品自营」文件体系，并为自营产品生成详情页。

#### 技术方案
1. **知识库结构**
   - `kb/Products/` 目录
   - 使用与藏品类似的 YAML frontmatter
   - 字段增加：价格（或面议）、库存、SKU、配送信息

2. **构建脚本扩展**
   - `build_kb.py` 增加对 `kb/Products/` 的扫描
   - 生成 `products.json` 和产品详情页

3. **前端页面**
   - 新增 `product-detail.html` 或在 `detail.html` 中支持产品类型
   - 增加「立即购买」/「联系咨询」按钮

#### 依赖
- 产品内容准备就绪
- 支付/用户系统（可选，可先只做咨询）

#### 验收标准
- [ ] 知识库中有产品文件
- [ ] 产品详情页展示完整信息
- [ ] 提供咨询或购买入口

#### 时间估算
- **1 周**

---

## 5. 第三阶段：远期探索

### 5.1 CLIP 语义以图搜图

**关联用户故事**：US-007

#### 目标
将当前的 pHash 以图搜图升级为 CLIP 语义相似。

#### 技术方案
- **本地模型**：使用 `open-clip` 或 `chinese-clip` 提取图片向量
- 构建图片向量索引（FAISS）
- 替换 `search_by_image_hash` 为 `search_by_image_clip`

#### 验收标准
- [ ] 上传图片后可返回语义相似的藏品
- [ ] 不只依赖构图/颜色

#### 时间估算
- **1–2 周**

---

### 5.2 语义搜索增强

#### 目标
通过 Kimi 增强语义搜索体验，如查询理解、结果摘要、语义改写。

#### 技术方案
- **查询改写**：用户输入「清代绣品」→ Kimi 扩展为「清代 刺绣 织物 服装」
- **结果摘要**：对搜索结果生成一段自然语言总结
- **Fallback 回答**：无结果时，由 Kimi 给出解释或建议

#### 验收标准
- [ ] 搜索结果质量提升
- [ ] 用户能感知到搜索更智能

#### 时间估算
- **1–2 周**

---

## 6. 里程碑规划

| 里程碑 | 时间 | 交付物 |
|-------|------|--------|
| M1：内容体验提升 | 第 1–2 周 | AI 评鉴全量生成、详情页推荐卡片、二维码占位、多语言框架 |
| M2：搜索与运营优化 | 第 3–4 周 | 标签推荐算法优化、数据分析页上线 |
| M3：商业化入口 | 第 5–7 周 | 真实二维码、自营产品详情页、在线支付 POC |
| M4：系统完善 | 第 8–10 周 | 用户系统、搜索服务部署、CMS 接入 |
| M5：智能升级 | 第 11–14 周 | CLIP 以图搜图、语义搜索增强 |

---

## 7. 风险管理

| 风险 | 影响 | 缓解措施 |
|-----|------|---------|
| Kimi API 限流或成本超预期 | AI 评鉴生成受阻 | 控制并发、分批生成、设置预算上限 |
| 批量生成评鉴质量不稳定 | 详情页内容参差不齐 | 增加提示词约束、人工抽检、后处理过滤 |
| 支付接入复杂度高 | 商业化延迟 | 先用「联系咨询」承接交易，支付作为增强 |
| 部署方案选择困难 | 上线延迟 | 先做静态 JSON 方案，验证后再升级实时服务 |

---

## 8. 相关文档

- `PRD.md` — 产品需求文档
- `USER_STORIES.md` — 用户故事
- `PROJECT_STATUS.md` — 项目当前状态
- `PROJECT_ISSUES.md` — 项目问题与风险
